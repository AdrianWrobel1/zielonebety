# PHASE 2.5: ARCHITECTURAL CONTRADICTION REGISTER
**Project:** `zielonebety1`  
**Status:** Authoritative Cross-Phase Inconsistency & Contradiction Audit  
**Evidence Level:** FACT-CODE & FACT-RUNTIME Verified  

---

## 1. Contradiction Register

| ID | Topic / Dimension | Claim A (Documentation / Early Stage) | Claim B (Source Code / Runtime Truth) | Evidence A | Evidence B | Actual Current Runtime Behavior | Severity | Action Required |
|:---:|:---|:---|:---|:---|:---|:---|:---:|:---|
| **C-01** | **Fetcher Log Meaning** | Fetcher logs state *"fetched N raw responses"* implying N network calls were made. | Fetcher issues $K \ll N$ network calls and reuses $N - K$ cached discovery payloads. | `SuperbetFetcher.fetch_event_data()` log statement. | `SuperbetFetcher.py:108` loops through `selected_event_ids`, reusing `item.metadata['raw']` for unselected IDs. | In a scan with 127 events and 8 selected detail IDs, exactly 8 HTTP requests are issued; 119 payloads are reused from discovery cache. | **INFORMATIONAL** | Documented in `PHASE2_5_ACQUISITION_ACCOUNTING.md`. Do not change log format before Phase 3. |
| **C-02** | **Horizon Filtering Location** | Horizon filtering is performed centrally by `DefaultEventSelectionPolicy`. | Betclic discovery also performs internal kickoff filtering before emitting items. | Phase 0 & Phase 2 architecture specs. | `providers/betclic/discovery/discovery.py:BetclicDiscovery._filter_events()` checks `hours_ahead` independently. | Discarded events are filtered in Betclic discovery and then filtered again in `EventSelectionPolicy`. | **P2** | Scheduled for consolidation under `EventSelectionPolicy` in Step 3 of Phase 3 roadmap. |
| **C-03** | **Market Breakdown Categorization** | Market breakdown is derived strictly from canonical market taxonomy. | Orchestrator maintains an inline string pattern matcher `_categorize_market_type()` for telemetry. | Phase 2 Taxonomy Spec. | `orchestration/scan_orchestrator.py:102-140` regex matching. | Telemetry breakdown uses inline helper while cross-matching uses `CanonicalMarketKey`. | **P1** | Scheduled for unification under `normalization.market_identity` in Step 2 of Phase 3 roadmap. |
| **C-04** | **ThreadPoolExecutor Worker Timeout Handling** | A worker task exceeding `worker_timeout` in `wait()` is marked as failed and discarded. | A worker task completing during graceful pool shutdown (`pool.shutdown(wait=True)`) is accepted as valid. | Phase 0 Concurrency Spec. | `orchestration/scan_orchestrator.py:898-904` checks `if fut in not_done:` and accepts valid `p_res`. | The orchestrator accepts completed provider results even if they completed during pool teardown. | **INFORMATIONAL** | Verified as working correctly in `PHASE2_5_CONCURRENCY_MODEL.md`. |
| **C-05** | **Player Name Normalization Implementation** | Player normalization is owned by `normalization.market_identity.normalize_player_name()`. | `scanner/prop_execution_matcher.py` has a secondary `_normalize_player_name()` helper. | `normalization/market_identity.py:145`. | `scanner/prop_execution_matcher.py:88`. | Both functions perform diacritic removal; core scanner pipeline calls `normalization.market_identity`. | **P2** | Scheduled for deduplication in Phase 3. |
| **C-06** | **Surebet Boundary on Equilibrium ($S = 1.0$)** | Arbitrage sum $S \le 1.0$ produces a surebet opportunity. | $S == 1.0$ is theoretical zero-margin equilibrium and is explicitly rejected as `NO_SUREBET`. | Early Stage 0 specifications. | `normalization/surebet.py:755-769` explicitly evaluates `elif arbitrage_sum == Decimal("1.0"): return NO_SUREBET`. | Zero-margin markets do not produce alerts; only $S < 1.0$ produces `SurebetOpportunity`. | **INFORMATIONAL** | Verified mathematically sound; zero-margin trades are not profitable after execution friction. |
